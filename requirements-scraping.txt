requests>=2.31
pandas>=2.0
beautifulsoup4>=4.12
lxml>=4.9

# Django (nécessaire pour manage.py import_data en CI)
Django>=4.2,<5.2
python-dotenv>=1.0

# Postgres (Supabase) en prod ; SQLite reste actif si DATABASE_URL absent.
psycopg[binary]>=3.2
dj-database-url>=2.2

# Requis au chargement de Django (urls.py importe transitivement
# dashboard.strategie_hmm). Toute commande manage.py instancie le HMM engine.
hmmlearn>=0.3
scikit-learn>=1.3
scipy>=1.11
