# Dépendances pour l'entraînement mensuel des modèles GARCH par action.
# Séparé de requirements-scraping.txt pour ne pas alourdir le pipeline quotidien
# (arch + scipy + statsmodels = ~80 Mo).

# Django + accès DB
Django>=4.2,<5.2
python-dotenv>=1.0

# Postgres (Supabase) en prod
psycopg[binary]>=3.2
dj-database-url>=2.2

# Requis au chargement de Django (urls.py importe transitivement
# dashboard.strategie_hmm). Toute commande manage.py instancie le HMM engine.
hmmlearn>=0.3
scikit-learn>=1.3

# Estimation GARCH/GJR-GARCH/EGARCH (Kevin Sheppard)
arch>=6.3
numpy>=1.24
pandas>=2.0
